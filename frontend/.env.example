# API - Must match backend PORT (3001) and API_VERSION (v1)
VITE_API_BASE_URL=http://localhost:3001/api/v1

# Supabase - Must match backend SUPABASE_URL (same project)
VITE_SUPABASE_URL=https://xxx.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# App always uses API; mock fallback is disabled in code.
# VITE_USE_MOCK is no longer used.

# ---------------------------------------------------------------------------
# Backend reference (set in backend/.env, not here):
# ---------------------------------------------------------------------------
# DATABASE_URL, DIRECT_URL - PostgreSQL connection
# SUPABASE_URL - Same as VITE_SUPABASE_URL above
# SUPABASE_SERVICE_ROLE_KEY - Server-only, never expose to frontend
# SUPABASE_JWT_SECRET - Server-only, for JWT verification
# FRONTEND_URL - CORS origin, e.g. http://localhost:5173 (Vite default)
# PORT - Backend port (3001)
# API_VERSION - API path segment (v1)
